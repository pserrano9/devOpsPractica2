---
- name: 'Podman'
  hosts: webservers
  remote_user: adminUsername
  become: true

   tasks:

   - name: 'Descargamos imagen que subiremos posteriormente a nuestro ACR'
    containers.podman.podman_image:
      name: mcr.microsoft.com/oss/bitnami/redis:6.0.8
